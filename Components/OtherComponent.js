import React, { Component } from "react";

export default class OtherComponent extends Component {
  render() {
    return (
      <div>This is the page that will be shown when we route to "/other"</div>
    );
  }
}
